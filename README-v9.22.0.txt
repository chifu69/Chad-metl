RP v9.22.0 — Eagle Identity & Conversation Context Fix

- First-person requests (I/me/my) are resolved against the signed-in account before conversation history.
- A previously viewed employee can no longer silently replace the signed-in user.
- Administrator/evaluator accounts without an associate competency record receive a clear clarification instead of another employee’s advancement data.
- Explicit employee questions still use the named employee.
- Advancement answers prioritize Critical Gates and show the first 5 priorities instead of flooding the chat with hundreds of requirements.
- Existing assignment, assessment, METL, permission, and workflow behavior retained.
