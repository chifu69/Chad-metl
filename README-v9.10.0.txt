RP v9.10.0
Based directly on RP v9.9.2 Assessment Search Plus Administration Export Fix.

Changes:
- Added explicit Start Assessment workflow after associate + METL task selection.
- Read-only users receive a personal readiness dashboard rather than plant/admin dashboard.
- Read-only corrective actions are view-only; Save/Close controls are not exposed.
- Eagle can use the signed-in read-only associate as context for self questions.
- Added a global browser download helper and repaired Backup creation feedback.
- Backup restore accepts both prior RP-IA-BACKUP packages and new RP-BACKUP packages.
- Service-worker cache bumped to v9.10.0.
